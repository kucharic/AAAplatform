#!/bin/bash

install()
{
    DEBIAN_FRONTEND=noninteractive

    if (( $(id -u) == 0 )) ; then
        apt-get update && apt-get install --reinstall -qy $* 
    else
        sudo apt-get update && apt-get install --reinstall -qy $* 
    fi
}